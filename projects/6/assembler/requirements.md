# Requirements

1. Accept single command line `prompt> HackAssembler Prog.asm`
2. The file may cointain a file path if not specified uses current folder
2. Creates single file Prog.hack with the binary, in the same folder, overwrites if existing

First stage: no symbols
Second stage: with symbols